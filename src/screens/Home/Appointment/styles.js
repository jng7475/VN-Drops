import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: { flex: 1 },
  // scrollView: {
  //   flex: 8,
  // },
  title: {
    fontWeight: 'bold',
    marginTop: '5%',
    textAlign: 'center',
    fontSize: 20,
    height: 31,
  },
});
